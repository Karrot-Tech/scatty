{
  "buildCommand": "cd ../.. && npm run build:shared && cd apps/mobile && npx expo export --platform web",
  "outputDirectory": "dist",
  "framework": null,
  "installCommand": "cd ../.. && npm install",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
