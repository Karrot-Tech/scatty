{
  "buildCommand": "npm run build:shared && cd apps/mobile && npx expo export --platform web",
  "outputDirectory": "apps/mobile/dist",
  "installCommand": "npm install",
  "framework": null,
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
